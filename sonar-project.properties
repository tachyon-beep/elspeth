# SonarQube/SonarCloud project settings.
# Project identifiers (override with -D flags if you need different values per run).
sonar.projectKey=tachyon-beep_elspeth
sonar.organization=tachyon-beep

# Direct the scanner to the Python source and test roots.
sonar.sources=src/elspeth
sonar.tests=tests

# Use the XML report emitted by pytest-cov (see pyproject addopts).
sonar.python.coverage.reportPaths=coverage.xml
