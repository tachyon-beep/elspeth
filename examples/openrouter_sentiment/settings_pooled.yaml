source:
  plugin: csv
  options:
    path: examples/openrouter_sentiment/input.csv
    schema:
      mode: fixed
      fields:
      - 'id: int'
      - 'text: str'
    on_validation_failure: discard
    on_success: output
transforms:
- plugin: openrouter_llm
  options:
    api_key: ${OPENROUTER_API_KEY}
    model: openai/gpt-4o-mini
    template: 'Analyze the sentiment of the following text and respond with ONLY a JSON object.


      Text: {{ row.text }}


      Respond with exactly this format (no other text):

      {"sentiment": "positive" or "negative" or "neutral", "confidence": 0.0-1.0, "summary": "brief explanation"}

      '
    temperature: 0.0
    response_field: sentiment_analysis
    required_input_fields:
    - text
    schema:
      mode: observed
    pool_size: 3
    on_success: output
sinks:
  output:
    plugin: json
    options:
      path: examples/openrouter_sentiment/output/results_pooled.json
      schema:
        mode: observed
      format: jsonl
landscape:
  url: sqlite:///examples/openrouter_sentiment/runs/audit_pooled.db
