# Phase 9 Doc-Correctness — Deferrals

Per Δ 9-5: factual issues noticed during the T1–T5 pass that are NOT among the five tensions and are therefore NOT fixed in this pass. Recorded here as input for a future doc-correctness round. One line per item; `[discovered while fixing T<N>]` annotation indicates which tension surfaced it.

- ARCHITECTURE.md At a Glance Python file count `315` is stale; current is `359` (`find src/elspeth -name '*.py' | wc -l`) — adjusted alongside LOC in this pass, but the doc's Components-section file mentions remain on older counts where present [discovered while fixing T4]
- ARCHITECTURE.md per-container LOC table (lines 153–164: CLI ~2,200, TUI ~800, MCP ~3,600, Engine ~12,000, Plugins ~20,600, Landscape ~8,300, Telemetry ~1,200, Checkpoint ~600, Rate Limiting ~300, Core ~5,000, Contracts ~8,300) is largely stale against the L1 verified counts in `02-l1-subsystem-map.md` — refresh row-by-row deferred to keep this pass minimal [discovered while fixing T4]
- ARCHITECTURE.md §3.1 Engine Components LOC entries (`KNOW-A26` Orchestrator ~3,500 / Processor ~1,860, `KNOW-A27` ExpressionParser ~652, `KNOW-A28` CoalesceExecutor ~1,054) diverge from the L1 file-LOC top-10 (`orchestrator/core.py` 3,281; `processor.py` 2,700; `coalesce_executor.py` 1,603) [discovered while fixing T4]
- ARCHITECTURE.md §3.2 Landscape Components claims `LandscapeRecorder ~1,040 LOC, 89 methods` and `ExecutionRepository ~1,480 LOC` (KNOW-A29/A31) but L1 measured `execution_repository.py` at 1,750 and `data_flow_repository.py` at 1,590 — Landscape internals have grown ~18% [discovered while fixing T4]
- ARCHITECTURE.md §3.3 Plugins per-category enumeration is stale: 4 sources (now 6: +`dataverse`, +`text`), 13 transforms (now 17: +`line_explode`, +`type_coerce`, +`value_transform`, +`rag_retrieval`), 4 sinks (now 6: +`chroma_sink`, +`dataverse`) [discovered while fixing T1]
- ARCHITECTURE.md §3.3 Plugins enumeration also predates the `azure_content_safety` / `azure_prompt_shield` rename (table still uses `content_safety` / `prompt_shield`); registry `name` attributes show the longer canonical names [discovered while fixing T1]
- ARCHITECTURE.md "How to Read This Document" / Table of Contents references the original 6-row ADR table; with 17 ADRs now present, an "Index of all ADRs" pointer to `docs/architecture/adr/README.md` would help auditors [discovered while fixing T2]
- ARCHITECTURE.md §Quality Assessment dates ("automated analysis 2026-03-26") are now > 1 month stale; risk-assessment table predates ADRs 007–017 entirely [discovered while fixing T2]
- ARCHITECTURE.md header "Last Updated 2026-04-03 synchronized with RC-5 branch" (KNOW-A8) is now stale relative to ADRs accepted 2026-04-19/20 — refreshing the header date is a separate concern [discovered while fixing T2]
- PLUGIN.md TransformResult section (KNOW-P12) and Plugin Registration section (KNOW-P22) reference `cli.py` `TRANSFORM_PLUGINS` registry — current code uses `discover_all_plugins()` discovery in `plugins/infrastructure/discovery.py`; the dual-registration claim in KNOW-P22 may itself be stale and warrants verification [discovered while fixing T1]
- PLUGIN.md Supported Types list (KNOW-P25: `str, int, float, bool, any`) does not mention nullable/optional or compound types if those are now supported — verification deferred [discovered while fixing T3]
- KNOW-A24 claims "21 audit tables"; this was not re-verified during T-pass and may itself have drifted as ADRs 010–017 introduced new contract-violation classes (some of which may have associated tables) [discovered while fixing T2]
- The frontend file count (`46 TypeScript/TSX, 1 CSS` / `47 frontend files`) was preserved verbatim in T4 because frontend was deferred at L1 to a frontend-aware archaeologist; whether it has drifted is unknown and should be remeasured by that follow-up pass [discovered while fixing T4]
