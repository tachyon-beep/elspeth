{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp-config.schema.json",
  "mcpServers": {
    "elspeth-landscape": {
      "command": "elspeth-mcp",
      "args": ["--database", "sqlite:///./state/audit.db"],
      "description": "ELSPETH Landscape audit database analysis - query runs, trace lineage, debug failures"
    }
  }
}
