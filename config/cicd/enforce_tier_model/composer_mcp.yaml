per_file_rules:
- pattern: composer_mcp/server.py
  rules:
  - R1
  reason: MCP tool arguments are external client data (Tier 3) - defensive access is correct
  expires: null
- pattern: composer_mcp/server.py
  rules:
  - R4
  reason: Broad except at MCP protocol boundary - all tool errors returned as text responses
  expires: null
  max_hits: 1
