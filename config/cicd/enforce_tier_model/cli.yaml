per_file_rules:
- pattern: cli.py
  rules:
  - R1
  - R4
  - R6
  reason: CLI handles user input, env vars, and external system checks (Tier 3)
  expires: null
- pattern: cli.py
  rules:
  - R5
  reason: Type discrimination for plugin dispatch, SQLite URI parsing, and raw YAML secrets validation
  expires: null
  max_hits: 4
- pattern: cli_helpers.py
  rules:
  - R1
  reason: resolve_audit_passphrase reads env vars (Tier 3 boundary) - .get() returns None to signal absent passphrase
  expires: null
