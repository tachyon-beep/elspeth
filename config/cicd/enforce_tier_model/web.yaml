allow_hits:
# Catalog routes — @router.get() false positives (FastAPI route decorators, not dict.get)
- key: web/catalog/routes.py:R1:list_sources:fp=ccdc6bb55bdef578
  owner: web-catalog
  reason: "False positive — @catalog_router.get() is FastAPI route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/catalog/routes.py:R1:list_transforms:fp=2c4051cabdd7abc4
  owner: web-catalog
  reason: "False positive — @catalog_router.get() is FastAPI route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/catalog/routes.py:R1:list_sinks:fp=2f3c4211eedd8864
  owner: web-catalog
  reason: "False positive — @catalog_router.get() is FastAPI route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/catalog/routes.py:R1:get_schema:fp=bfaf3a79a383cb6f
  owner: web-catalog
  reason: "False positive — @catalog_router.get() is FastAPI route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

# Catalog service — Tier 3 JSON schema introspection (.get() on Pydantic-generated JSON schema dicts)
- key: web/catalog/routes.py:R1:get_schema:fp=bc80b0854f0cb34e
  owner: web-catalog
  reason: "Tier 3 boundary — plugin_type from URL path parameter, lookup in constant dict"
  safety: "Returns None on miss, handled by 404 response"
  expires: null

# CatalogServiceImpl — silent-except for unknown plugin type fallback (pre-existing)
- key: web/catalog/service.py:R6:CatalogServiceImpl:_resolve_config_model:fp=5e0056175f439976
  owner: web-catalog
  reason: "Expected error path — UnknownPluginTypeError caught and returns None to caller"
  safety: "Caller checks None return and skips config extraction; no silent data loss"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=4ff2ba785b505451
  owner: web-catalog
  reason: "Tier 3 boundary — Pydantic-generated JSON schema 'properties' key may be absent"
  safety: "Default empty dict; no properties means no config fields"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=1f5f1cda6e9c0466
  owner: web-catalog
  reason: "Tier 3 boundary — Pydantic JSON schema 'required' key may be absent"
  safety: "Default empty list; no required fields"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=1de9302972b26a3a
  owner: web-catalog
  reason: "Tier 3 boundary — JSON schema field 'type' may be absent (e.g. anyOf unions)"
  safety: "Default 'object'; conservative fallback"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=ff5c2a0e7c73ce66
  owner: web-catalog
  reason: "Tier 3 boundary — JSON schema anyOf branch may lack 'type'"
  safety: "Checked before use; null branch skipped"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=e3cf8be3854ff16b
  owner: web-catalog
  reason: "Tier 3 boundary — JSON schema branch 'type' checked for null filtering"
  safety: "Skips null branches; extracts real type from remaining"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=eddfe72d549a8215
  owner: web-catalog
  reason: "Tier 3 boundary — fallback type extraction from anyOf branch"
  safety: "Default 'object'; conservative"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=4fcdbac964375893
  owner: web-catalog
  reason: "Tier 3 boundary — JSON schema field 'description' is optional"
  safety: "Returns None; ConfigFieldInfo.description accepts None"
  expires: null

- key: web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=a288b28b42de381b
  owner: web-catalog
  reason: "Tier 3 boundary — JSON schema field 'default' is optional"
  safety: "Returns None; ConfigFieldInfo.default accepts None"
  expires: null

# JWKSTokenValidator — httpx client.get() false positives (HTTP method, not dict.get)
- key: web/auth/oidc.py:R1:JWKSTokenValidator:ensure_jwks:fp=5d9ec6c99446cd42
  owner: web-auth
  reason: "False positive — httpx.AsyncClient.get() is an HTTP GET request, not dict.get()"
  safety: HTTP errors caught by raise_for_status() and except block
  expires: null

- key: web/auth/oidc.py:R1:JWKSTokenValidator:ensure_jwks:fp=0212bb5677a2406e
  owner: web-auth
  reason: "False positive — httpx.AsyncClient.get() is an HTTP GET request, not dict.get()"
  safety: HTTP errors caught by raise_for_status() and except block
  expires: null

# JWKSTokenValidator — Tier 3 JWT header .get() for kid extraction
- key: web/auth/oidc.py:R1:JWKSTokenValidator:decode_token:fp=43b5f14ace609f1d
  owner: web-auth
  reason: "Tier 3 boundary — JWT header kid field is optional per RFC 7515"
  safety: "Missing kid falls through to first key in JWKS; AuthenticationError on decode failure"
  expires: null

# OIDC provider — Tier 3 JWT payload .get() calls (external IdP token claims)
- key: web/auth/oidc.py:R1:OIDCAuthProvider:authenticate:fp=a8c6a2b695a3d754
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username is optional in OIDC tokens, falls back to sub"
  safety: "Default is payload['sub'] which is required; absence of optional claim is expected"
  expires: null

- key: web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=a3787dad45249f67
  owner: web-auth
  reason: "Tier 3 boundary — groups claim is optional in OIDC tokens; None = no groups"
  safety: "None returns empty groups; non-list raises AuthenticationError; no silent discard"
  expires: null

- key: web/auth/oidc.py:R5:OIDCAuthProvider:get_user_info:fp=8ca64d6c43cc262d
  owner: web-auth
  reason: "Tier 3 boundary — groups claim type is not guaranteed by external IdP"
  safety: "Non-list raises AuthenticationError with type info for debugging"
  expires: null

- key: web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=e9a89464d499fed3
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username is optional in OIDC tokens, falls back to sub"
  safety: "Default is payload['sub'] which is required"
  expires: null

- key: web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=50e2c5aea8128cfe
  owner: web-auth
  reason: "Tier 3 boundary — name claim is optional, falls back to preferred_username"
  safety: "Falls back to preferred_username or 'Unknown'; never to opaque sub UUID"
  expires: null

- key: web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=64985ed62a0e03ec
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username fallback in display_name chain"
  safety: "Part of name -> preferred_username -> 'Unknown' chain"
  expires: null

- key: web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=f2a1b1758a966c9d
  owner: web-auth
  reason: "Tier 3 boundary — email claim is optional in OIDC tokens"
  safety: "Default None; UserProfile.email accepts None; absence is honest representation"
  expires: null

# Entra provider — Tier 3 JWT payload .get() and isinstance() calls
- key: web/auth/entra.py:R1:EntraAuthProvider:_extract_groups:fp=268368c0e155cc41
  owner: web-auth
  reason: "Tier 3 boundary — groups claim is optional in Entra tokens; None = no groups"
  safety: "None returns empty; non-list raises AuthenticationError; no silent discard"
  expires: null

- key: web/auth/entra.py:R5:EntraAuthProvider:_extract_groups:fp=6dcc1eafd56c6429
  owner: web-auth
  reason: "Tier 3 boundary — groups claim type is not guaranteed by external IdP"
  safety: "Non-list raises AuthenticationError with type info"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:_extract_groups:fp=cc258a6069465936
  owner: web-auth
  reason: "Tier 3 boundary — roles claim is optional in Entra tokens; None = no roles"
  safety: "None returns empty; non-list raises AuthenticationError; no silent discard"
  expires: null

- key: web/auth/entra.py:R5:EntraAuthProvider:_extract_groups:fp=8a85154b5c04b93d
  owner: web-auth
  reason: "Tier 3 boundary — roles claim type is not guaranteed by external IdP"
  safety: "Non-list raises AuthenticationError with type info"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:authenticate:fp=fd48174d76f75c9d
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username is optional in Entra tokens, falls back to sub"
  safety: "Default is payload['sub'] which is required; absence of optional claim is expected"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=011fa8f44233c8e6
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username is optional in Entra tokens, falls back to sub"
  safety: "Default is payload['sub'] which is required"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=cf8febe0ecd5d91c
  owner: web-auth
  reason: "Tier 3 boundary — email claim is optional in Entra tokens"
  safety: "Default None; UserProfile.email accepts None"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=b006a5696f76b645
  owner: web-auth
  reason: "Tier 3 boundary — name claim is optional, falls back to preferred_username"
  safety: "Falls back to preferred_username or 'Unknown'; never to opaque sub UUID"
  expires: null

- key: web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=a3dbe8d2b3f309d5
  owner: web-auth
  reason: "Tier 3 boundary — preferred_username fallback in display_name chain"
  safety: "Part of name -> preferred_username -> 'Unknown' chain"
  expires: null

# Auth routes — @router.get() false positives (FastAPI route decorators, not dict.get)
- key: web/auth/routes.py:R1:create_auth_router:auth_config:fp=3f8fc32990ce6112
  owner: web-auth
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved; decorator syntax triggers text-based pattern match
  expires: null

- key: web/auth/routes.py:R1:create_auth_router:me:fp=4db7c70c9126b330
  owner: web-auth
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved; decorator syntax triggers text-based pattern match
  expires: null

# SessionServiceImpl — _unwrap_envelope checks envelope structure (Tier 1 read boundary)
- key: web/sessions/service.py:R5:SessionServiceImpl:_unwrap_envelope:fp=1f13048cd351a07f
  owner: web-sessions
  reason: "Tier 1 read boundary — validates JSON envelope structure before accessing _version key"
  safety: "Crashes on unknown version or missing envelope; no silent coercion"
  expires: null

# App factory — os.environ.get() and @app.get() false positives
- key: web/app.py:R1:create_app:fp=fffa82ecffb3c21f
  owner: web-app
  reason: "Tier 3 boundary — os.environ.get() for env var check, not dict.get() on pipeline data"
  safety: "Guards dev-mode secret_key warning; no data flow impact"
  expires: null

- key: web/app.py:R1:create_app:health:fp=9f0230766ffa4cd3
  owner: web-app
  reason: "False positive — @app.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/app.py:R1:create_app:system_status:fp=5b72498a2aa7b56d
  owner: web-app
  reason: "False positive — @app.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

# Session routes — @router.get() false positives (FastAPI route decorators)
- key: web/sessions/routes.py:R1:create_session_router:list_sessions:fp=decbc54c94c75e3c
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/sessions/routes.py:R1:create_session_router:get_session:fp=9cf17bef7e30db48
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/sessions/routes.py:R1:create_session_router:get_messages:fp=7ceeee2a5e642481
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/sessions/routes.py:R1:create_session_router:get_current_state:fp=fb7810da03bd8b01
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

# Execution routes — FastAPI decorators and WebSocket transport/auth handling
- key: web/execution/routes.py:R1:create_execution_router:get_run_status:fp=d08d1e4253439ed0
  owner: web-execution
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/execution/routes.py:R1:create_execution_router:get_run_results:fp=f7d5bcef727d0b7d
  owner: web-execution
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/execution/routes.py:R4:create_execution_router:websocket_run_progress:fp=39b0b5f635b0c602
  owner: web-execution
  reason: "WebSocket auth boundary — auth providers may fail with provider-specific exceptions beyond AuthenticationError"
  safety: "Any failure is handled by denying the connection with close code 4001 before websocket.accept()"
  expires: null

- key: web/execution/routes.py:R4:create_execution_router:websocket_run_progress:fp=f39f2a57045dcacd
  owner: web-execution
  reason: "Run-ownership gate on WebSocket setup treats backend lookup failures as not-found to avoid leaking run existence"
  safety: "Failure closes the socket with 4004 and does not expose run metadata"
  expires: null

- key: web/execution/routes.py:R1:create_execution_router:websocket_run_progress:fp=edcad716854e9464
  owner: web-execution
  reason: "False positive — asyncio.Queue.get() is an async queue method, not dict.get()"
  safety: "Queue timeout is handled explicitly by heartbeat logic"
  expires: null

- key: web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=ca6a807e18a9c081
  owner: web-execution
  reason: "Heartbeat timeout is an expected idle path, not an error swallow"
  safety: "The handler emits an explicit heartbeat frame and continues waiting, so clients get a visible keepalive signal"
  expires: null

- key: web/execution/routes.py:R4:create_execution_router:websocket_run_progress:fp=c4039cf10d17cb30
  owner: web-execution
  reason: "WebSocket transport writes can fail with varied connection-state exceptions across ASGI servers"
  safety: "Failure breaks the loop and cleanup runs in finally; no run data is lost or fabricated"
  expires: null

- key: web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=f70460a861072998
  owner: web-execution
  reason: "WebSocketDisconnect is the expected disconnect control-flow path"
  safety: "Disconnect falls through to finally for unsubscribe cleanup; no error is hidden from callers"
  expires: null

- key: web/execution/routes.py:R4:create_execution_router:websocket_run_progress:fp=8051d9a2c963899b
  owner: web-execution
  reason: "Top-level WebSocket handler guard converts unexpected transport/runtime failures into a clean 1011 close"
  safety: "The error is logged and the client receives an explicit internal-error close code"
  expires: null

- key: web/execution/routes.py:R4:create_execution_router:websocket_run_progress:fp=3e188822ed707289
  owner: web-execution
  reason: "Best-effort websocket.close() can raise transport-specific exceptions after a primary handler failure"
  safety: "Secondary close failure is logged only after the original failure path is already established"
  expires: null

# Execution service — optional config keys and concurrent shutdown-event lifecycle
- key: web/execution/service.py:R1:ExecutionServiceImpl:execute:fp=05f6bf7c01106310
  owner: web-execution
  reason: "Session composition state may legitimately omit optional source path/file keys"
  safety: "Only present keys are checked against the uploads allowlist; missing keys skip the check without fabricating values"
  expires: null

- key: web/execution/service.py:R9:ExecutionServiceImpl:execute:fp=52f2e397f8f0ae3f
  owner: web-execution
  reason: "Shutdown-event cleanup is idempotent because submit failure can race with cancellation/lifecycle cleanup"
  safety: "Missing key means there is no active event to clean; run creation aborts immediately"
  expires: null

- key: web/execution/service.py:R1:ExecutionServiceImpl:cancel:fp=efc0a289072bef70
  owner: web-execution
  reason: "Cancellation checks whether a live shutdown event exists before falling back to pending/terminal-run handling"
  safety: "Missing event routes to an explicit database-backed status update path"
  expires: null

- key: web/execution/service.py:R4:ExecutionServiceImpl:_run_pipeline:_safe_broadcast:fp=1f95d33248434c3d
  owner: web-execution
  reason: "Progress broadcasting is operational visibility and may fail with transport/runtime exceptions outside pipeline correctness"
  safety: "Broadcast failure is logged while run status and audit updates continue on the authoritative path"
  expires: null

- key: web/execution/service.py:R5:ExecutionServiceImpl:_run_pipeline:fp=33fc2673c9f0be24
  owner: web-execution
  reason: "BaseException split distinguishes process-termination control flow from ordinary failures"
  safety: "KeyboardInterrupt/SystemExit are re-raised and skip the best-effort failed-status write to preserve shutdown semantics"
  expires: null

- key: web/execution/service.py:R4:ExecutionServiceImpl:_run_pipeline:fp=75a503441a2f61b8
  owner: web-execution
  reason: "Failure while marking a crashed run as failed is a secondary cleanup error on an already-failing path"
  safety: "Secondary failure is logged and the original exception continues to propagate"
  expires: null

- key: web/execution/service.py:R9:ExecutionServiceImpl:_run_pipeline:fp=e25340ab9894eff7
  owner: web-execution
  reason: "Final shutdown-event cleanup is intentionally idempotent because completion and cancellation can race"
  safety: "Removing a missing key is harmless and avoids masking the primary pipeline outcome"
  expires: null

# Execution progress — subscriber map lifecycle helpers
- key: web/execution/progress.py:R8:ProgressBroadcaster:subscribe:fp=4e52b3400d28695a
  owner: web-execution
  reason: "Subscriber map creates the per-run queue set lazily on first subscription"
  safety: "Mutation is guarded by the broadcaster lock and only initializes empty internal state"
  expires: null

- key: web/execution/progress.py:R1:ProgressBroadcaster:broadcast:fp=9e876f45c424ae5e
  owner: web-execution
  reason: "Broadcast treats an absent run entry as 'no subscribers' for that run"
  safety: "A missing subscriber set results in a no-op broadcast, which is the correct behavior"
  expires: null

- key: web/execution/progress.py:R9:ProgressBroadcaster:cleanup_run:fp=743167bc43d078f7
  owner: web-execution
  reason: "Run cleanup is idempotent because unsubscribe and explicit cleanup can both remove the same run entry"
  safety: "Missing key simply means there are no remaining subscribers to clean up"
  expires: null

# Execution validation — optional path/file keys in user-authored source config
- key: web/execution/validation.py:R1:validate_pipeline:fp=f4448c9a506ce863
  owner: web-execution
  reason: "Validation checks optional source path/file keys supplied by composition state"
  safety: "Absent keys are treated honestly as 'no path provided' and the check is recorded as skipped/passed"
  expires: null

- key: web/sessions/routes.py:R1:create_session_router:get_state_versions:fp=6e1f4f1fc01319f9
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

- key: web/sessions/routes.py:R1:create_session_router:get_state_yaml:fp=a709e6420dc9571e
  owner: web-sessions
  reason: "False positive — @router.get() is FastAPI HTTP route decorator, not dict.get()"
  safety: No dict access involved
  expires: null

# Session routes — broad except in convergence partial state guards (F2 hardening)
- key: web/sessions/routes.py:R4:create_session_router:send_message:fp=f6fed8b0740f3d3d
  owner: web-composer
  reason: "Convergence validation guard — if validate() raises, degrade to is_valid=False rather than lose partial state"
  safety: "Caught exception produces ValidationSummary(is_valid=False); state still persisted"
  expires: null

- key: web/sessions/routes.py:R4:create_session_router:send_message:fp=f8cc5e458c655b2c
  owner: web-composer
  reason: "Convergence persistence guard — if save_composition_state() fails, log and return 422 without partial_state"
  safety: "Caught exception logged via structlog; 422 response omits partial_state; no crash to 500"
  expires: null

# Auth middleware — Tier 3 HTTP header access (Authorization header from client)
- key: web/auth/middleware.py:R1:get_current_user:fp=27850ac518f1116d
  owner: web-auth
  reason: "Tier 3 boundary — request.headers.get('Authorization') reads untrusted HTTP header"
  safety: "Missing header returns 401; malformed header returns 401; no default fabrication"
  expires: null

# Composer state — NodeSpec.from_dict() uses .get() for optional fields absent from to_dict()
- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=cfc303e4ee462b63
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=8c8f945cb356cd6b
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=54276a836e71e2ad
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=1a3dc41d13b14500
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=0fc6469ca0fca4c3
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

- key: web/composer/state.py:R1:NodeSpec:from_dict:fp=d9e79308f2030bf0
  owner: web-composer
  reason: "Optional fields — to_dict() conditionally omits None fields; from_dict() defaults to None"
  safety: "Missing = None; matches dataclass field default; round-trip invariant tested"
  expires: null

# Composer state — with_metadata() uses .get() for partial dict patch
- key: web/composer/state.py:R1:CompositionState:with_metadata:fp=a72cd948565706e7
  owner: web-composer
  reason: "Partial patch API — only provided fields are updated, others preserved"
  safety: "Defaults are the current field values; no fabrication, no loss"
  expires: null

- key: web/composer/state.py:R1:CompositionState:with_metadata:fp=fc470cb12ec23522
  owner: web-composer
  reason: "Partial patch API — only provided fields are updated, others preserved"
  safety: "Defaults are the current field values; no fabrication, no loss"
  expires: null

# Session service — IntegrityError handling for upsert-or-skip patterns
- key: web/sessions/service.py:R6:SessionServiceImpl:save_composition_state:_sync:fp=c07f71670e637c5c
  owner: web-sessions
  reason: "Upsert-or-skip — IntegrityError on duplicate version is expected race condition, not a bug"
  safety: "Returns None to caller; caller retries or skips; no data loss"
  expires: null

- key: web/sessions/service.py:R6:SessionServiceImpl:set_active_state:_sync:fp=7881a6e600190df3
  owner: web-sessions
  reason: "Upsert-or-skip — IntegrityError on duplicate active state is expected race condition"
  safety: "Returns None to caller; caller retries or skips; no data loss"
  expires: null

per_file_rules:
# Composer tools — mutation executors parse LLM tool call arguments (Tier 3).
- pattern: web/composer/tools.py
  rules:
  - R1
  - R5
  reason: "Tier 3 boundary — LLM tool call arguments are external data with optional fields per JSON schema"
  expires: null

- pattern: web/composer/service.py
  rules:
  - R6
  reason: "Tier 3 boundary — LLM tool call arguments may contain malformed JSON (JSONDecodeError) or missing required keys (KeyError/TypeError). Errors returned to LLM for self-correction, not swallowed."
  expires: null
