name: Enforce composer catch-order discipline

on:
  pull_request:
    paths:
      - "src/elspeth/web/**/*.py"
      - "config/cicd/enforce_composer_catch_order/**"
      - "scripts/cicd/enforce_composer_catch_order.py"
      - ".github/workflows/enforce-composer-catch-order.yaml"
  push:
    branches:
      - main
    paths:
      - "src/elspeth/web/**/*.py"
      - "config/cicd/enforce_composer_catch_order/**"
      - "scripts/cicd/enforce_composer_catch_order.py"

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: |
          python -m pip install --upgrade pip
          pip install pyyaml
      - run: |
          python scripts/cicd/enforce_composer_catch_order.py check \
            --root src/elspeth \
            --allowlist config/cicd/enforce_composer_catch_order
